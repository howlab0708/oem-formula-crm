node_modules
.next
out
next-env.d.ts
*.tsbuildinfo
.DS_Store
.env*.local

# Claude Code 개인 설정(권한 허용 규칙). 공유하지 않는다.
.claude/settings.local.json

# 공장에서 받은 실제 견적서 숫자(원료단가·가공비·마진). 저장소가 공개라 올리지 않는다.
# scripts/verify-formula-calc.mjs 가 이 파일이 있을 때만 실제 금액과 대조한다.
fixtures/*.local.json

# 배포별 로그인 비밀번호. 저장소가 공개라 한 번 올라가면 이력에서 지워도 이미 새어 나간다.
# 비밀번호는 Vercel 환경변수로만 넣고, 이 파일은 로컬에만 둔다.
비밀번호.txt
